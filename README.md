# hynetworkx
A network repository that handles graphs, hypergraphs, and network analyses thereof.

## Main classes
- `Vertex()`
- `VertexSet()`
- `Hyperedge()`
- `HyperedgeSet()`
- `IncidenceMatrix()`
- `Hypergraph()`
